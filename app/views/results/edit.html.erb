<h1>Editing result</h1>
<%= form_for(:result, :url => goal_result_path(@goal, @result), :html => { :method => :put }) do |f| %>
  <% if @goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goal.errors.count, "error") %> prohibited this result from being saved:</h2>

      <ul>
      <% @goal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
<%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Back', @goal %>
